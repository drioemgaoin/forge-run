// Job definition generation (scenario selection, deterministic mapping).
// No implementation yet.

pub trait JobDefinitionGenerator {
    // fn generate(&self, input: JobDefinitionInput) -> JobDefinition;
}
